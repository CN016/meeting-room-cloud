<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="meeting.room.meetingroomcloud.dao.TeacherInfoMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="meeting.room.meetingroomcloud.entity.TeacherInfo">
        <id column="id" property="id" />
        <result column="is_first" property="isFirst" />
        <result column="is_prove" property="isProve" />
        <result column="lab" property="lab" />
        <result column="openid" property="openid" />
        <result column="role" property="role" />
        <result column="teacher_email" property="teacherEmail" />
        <result column="teacher_name" property="teacherName" />
        <result column="teacher_phone" property="teacherPhone" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, is_first, is_prove, lab, openid, role, teacher_email, teacher_name, teacher_phone
    </sql>

</mapper>
